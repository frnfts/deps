plugins {
    id 'base'

    // Apply the java plugin to add support for Java
    id 'java'
}

allprojects {
    group = 'com.sample'
    version = '1.0'
    repositories {
        jcenter()
	mavenCentral() 
    } 
}

dependencies {
   implementation project(':java')
}
